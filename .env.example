# Leave empty for combined deployment (Docker) or local dev with Vite proxy
# Set to backend URL for separate deployments (e.g. Vercel + Render):
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
